#!/bin/zsh
set -euo pipefail

SCRIPT_PATH="${0:A}"
ROOT_DIR="$(cd "$(dirname "$SCRIPT_PATH")/.." && pwd)"
source "$ROOT_DIR/scripts/lib/automation_common.sh"

if ! resolve_python; then
  exit 127
fi

cd "$ROOT_DIR"
exec "$PYTHON_BIN" -m tools.icloud_photo_sync.cli "$@"
